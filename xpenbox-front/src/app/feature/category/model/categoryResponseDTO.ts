/**
 * DTO representing the response structure for a category.
 * Includes resource code, name, color, and state.
 */
export interface CategoryResponseDTO {
    resourceCode: string;
    name: string;
    color: string;
    state: boolean;
}
