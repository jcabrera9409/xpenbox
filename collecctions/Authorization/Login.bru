meta {
  name: Login
  type: http
  seq: 6
}

post {
  url: {{apiUrl}}/api/rest/v1/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{email}}",
    "password": "{{password}}",
    "rememberMe": false
  }
}
